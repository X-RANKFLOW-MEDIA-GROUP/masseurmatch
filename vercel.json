{
  "installCommand": "corepack enable && corepack prepare pnpm@10.32.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.32.1 --activate && pnpm run release:check",
  "framework": "nextjs",
  "ignoreCommand": "node scripts/vercel-ignore-build.mjs",
  "crons": [
    {
      "path": "/api/migrate/process",
      "schedule": "0 6 * * *"
    }
  ]
}
